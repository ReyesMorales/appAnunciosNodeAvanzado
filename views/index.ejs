<!DOCTYPE html>
<html>

<head>
	<title>Nodepop</title>
	<style>
		img.anuncio-img {
		  max-width: 100%;
		  max-height: 200px;
		}
	  </style>
</head>

<body>
	<h1> <%= __('Welcome to our adverts section') %> </h1>
	<% anuncios.forEach(anuncio=> { %>
		<div>
			<h2>
				<%= anuncio.titulo %>
			</h2>
			<p><strong> <%=__('Description') %>:</strong>
				<%= anuncio.descripcion %>
			</p>
			<p><strong> <%=__('Type') %>:</strong>
				<%= anuncio.tipo %>
			</p>
			<p><strong> <%=__('Price') %>:</strong>
				<%= anuncio.precio %>
			</p>
			<% if (anuncio.foto) { %>
				<img class="anuncio-img" src="<%= anuncio.foto %>" alt="Foto del anuncio">
				<% } %>
					<p><strong> <%=__('Tags') %>:</strong>
						<% anuncio.tags.forEach(tag=> { %>
							<%= tag.nombre %>
								<% }); %>
					</p>
		</div>
		<% }); %>
</body>

</html>